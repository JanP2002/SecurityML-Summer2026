# ==============================================================
# CH2 — Anomaly Detection: Requirements
# ==============================================================
#
# Setup instructions (from the CH2/ directory):
#
#   1. Create and activate a virtual environment:
#        python -m venv venv
#        # Windows:
#        venv\Scripts\activate
#        # Linux / macOS:
#        source venv/bin/activate
#
#   2. Install PyTorch with CUDA 12.8 support (must be done FIRST,
#      before the rest of this file, because the package index differs):
#        pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
#
#   3. Install all other dependencies:
#        pip install -r requirements.txt
#
# If you do NOT have an NVIDIA GPU or a different CUDA version,
# replace cu128 in the URL above with cpu or your CUDA version
# (e.g. cu121 for CUDA 12.1).
# ==============================================================

# Deep learning framework (installed separately — see note above)
torch>=2.0.0
torchvision>=0.15.0

# Numerical computing
numpy>=1.24.0

# Data science / evaluation
scikit-learn>=1.3.0

# Image processing (used by load_pythia_data for PNG loading)
Pillow>=9.5.0

# Visualisation
matplotlib>=3.7.0
